language: python

python:
  - "2.7"
  - "3.3"
  - "3.4"
  - "3.5"

env:
  - REQUESTS="requests==2.4.2"
  - REQUESTS="requests"

install:
  - pip install $REQUESTS pytest-cov codecov

script:
  - py.test -v --cov

after_success:
  - codecov
